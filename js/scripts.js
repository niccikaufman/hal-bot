function passwordParser(numbers) {
  if(parseInt(numbers) === 1) {
  return "beep";
  } else if(parseInt(numbers) === 2) {
    return "boop";
  } else if(parseInt(numbers) === 3) {
    return "I'm sorry, Dave. I'm afraid I can't do that.";
  }
}